<div id="menu">
  <nav class="md:hidden py-6 px-6 border-b">
    <div class="flex items-center justify-between">
        <a href="/" class="text-lg font-semibold tracking-widest text-gray-900 uppercase rounded-lg dark-mode:text-white focus:outline-none focus:shadow-outline">
            <img src="/img/logo/norm-64.png" class="inline w-10 h-10"/> Aegir
        </a>
      <button class="navbar-burger flex items-center rounded focus:outline-none">
        <svg class="text-white bg-indigo-500 hover:bg-indigo-600 block h-8 w-8 p-2 rounded" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg" fill="currentColor">
          <title>Mobile menu</title>
          <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"></path>
        </svg>
      </button>
    </div>
  </nav>
  <div class="hidden md:block navbar-menu relative z-50">
    <div class="navbar-backdrop fixed md:hidden inset-0 bg-gray-800 opacity-10"></div>
    <nav class="fixed top-0 left-0 bottom-0 flex flex-col w-3/4 md:w-60 sm:max-w-xs pt-6 pb-8 bg-white border-r overflow-y-auto">
      <div class="flex w-full items-center px-6 pb-6 mb-6 lg:border-b border-blue-50">
        <a href="/" class="text-lg font-semibold tracking-widest text-gray-900 uppercase rounded-lg dark-mode:text-white focus:outline-none focus:shadow-outline">
            <img src="/img/logo/norm-64.png" class="inline w-10 h-10"/> Aegir
        </a>
      </div>
      <div class="px-4 pb-6">
        <div v-for="s in sections">
        <h3 class="mb-2 text-xs uppercase text-gray-200 font-medium">{{ s }}</h3>
        <ul class="mb-8 text-sm font-medium text-gray-500">
          <li v-for="l in section(s)">
            <a class="flex items-center pl-3 py-3 pr-4 hover:bg-indigo-50 hover:text-gray-500 rounded" :class="{'text-gray-50 bg-purple-500':activeLink(l.l)}" :href="l.l">
              <span>{{ l.t }}</span>
            </a>
          </li>
        </ul>
        </div>
      </div>
    </nav>
  </div>
  <div class="mx-auto md:ml-60"></div>
</div>